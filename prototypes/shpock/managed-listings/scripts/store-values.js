function storeTitle(titleValue) {
    localStorage.setItem('storedTitle', titleValue)   
}

function storeDescription(descriptionValue) {
    localStorage.setItem('storedDescription', descriptionValue)   
}

function storePrice(priceValue) {
    localStorage.setItem('storedPrice', priceValue)   
}